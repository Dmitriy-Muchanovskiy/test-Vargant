vagrant up
vagrant ssh vm1 -c 'curl http://192.168.1.12'
В теории 1 команда должна создавать 2 виртуальные машины, в файле Vagrant добавлено исользование Ansible,через него установятся nginx и curl
После запуска VM, вторая команда через vagrant подключится к vm1 и выполнит через установелнный curl команду.
Но у себя не смог проверить точно ли Ansible всё правильно увидит, т.к. дома нет железного ПК с линуксом, а на вируалке вторая виртуалка не хочет нормально разворачиватся. 
В мануале по Vagrant говорилось, что он должен сам создать hosts и inventory, чтобы Ansible ими воспользовался.
